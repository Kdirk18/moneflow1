// components/WhiteBox.js
import React from 'react';

const WhiteBox = () => {
    return (
        <div style={styles.container}>
            {/* Nội dung của WhiteBox */}
        </div>
    );
};

const styles = {
    container: {
        width: '557px',
        height: '100px',
        backgroundColor: '#fff',
        boxShadow: '0px 4px 6px rgba(0, 0, 0, 0.1)',
        margin: 'auto',
    },
};

export default WhiteBox;
